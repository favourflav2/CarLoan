import * as React from 'react';
import { PieChart } from '@mui/x-charts/PieChart';

export interface IPieChartCompProps {
}

export default function PieChartComp (props: IPieChartCompProps) {
  return (
    <div>
      
    </div>
  );
}
